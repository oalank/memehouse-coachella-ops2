# For local dev: point to your Railway API
# Get URL from Railway Dashboard → your service → Settings → Networking
VITE_API_URL=https://memehouseops-production.up.railway.app

# Demo-only (dev): bypass login and open dashboard directly. Remove or leave unset for real auth.
# VITE_DEMO_BYPASS_AUTH=true
